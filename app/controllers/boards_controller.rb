class BoardsController < ApplicationController
    #controller for the project boards
    def index
        #by blog tutorial, this is not supposed to be a thing, look into it later
        #render('new')
    end
end